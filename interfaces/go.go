package main

import (
	"fmt"
	"math"
)

type geometry interface {
	area() float64
	perim() float64
}

type rect struct {
	width, height float64
}
type circle struct {
	radius float64
}

func (r rect) area() float64 {
	return r.width * r.height
}
func (r rect) perim() float64 {
	return 2*r.width + 2*r.height
}

func (c circle) area() float64 {
	return math.Pi * c.radius * c.radius
}
func (c circle) perim() float64 {
	return 2 * math.Pi * c.radius
}

func measure(g geometry) {
	//Aqui ele faz uso dos métodos que as interfaces de circle e rect compartilham
	//que é area e perim
	//BEM SIMILAR AO FACTORY DESIGN PATTERN
	//Pois você apenas dá o material, e a fabrica ja pode produzir o resultado
	//Para saber mais de interfaces:
	//https://jordanorelli.com/post/32665860244/how-to-use-interfaces-in-go
	fmt.Println(g)
	fmt.Println(g.area())
	fmt.Println(g.perim())
}

func main() {
	r := rect{width: 3, height: 4}
	c := circle{radius: 5}

	measure(r)
	measure(c)
}
